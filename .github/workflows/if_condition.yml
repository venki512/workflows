name: Conditional Workflow Test
on: push
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echos "Hello"
  Deploy:
    needs: Test
    runs-on: ubuntu-latest
    steps:
      - name: Run after test
        run: echo "Test is success"
