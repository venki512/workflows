name: Conditional Workflow Test
on: push
jobs:
  Test:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echos "Hello"
  Deploy:
    needs: Test
    runs-on: ubuntu-latest
    steps:
      - name: Run after test
        run: echo "Test is success"
